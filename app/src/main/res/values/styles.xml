<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/activity_background</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/ActionModeStyle</item>
    </style>

    <style name="ActionModeStyle" parent="Widget.AppCompat.ActionMode">
        <item name="background">@color/action_mode_status_bar_color</item>
        <item name="backgroundSplit">@color/action_mode_status_bar_color</item>
        <item name="titleTextStyle">@style/ActionModeStyle.TitleTextStyle</item>
        <item name="android:actionModeCloseDrawable">@drawable/ic_arrow_back</item>
        <item name="actionModeCloseDrawable">@drawable/ic_arrow_back</item>
    </style>

    <style name="ActionModeStyle.TitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionMode.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="ToolbarStyle" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="SaveButtonStyle">
        <item name="android:background">@drawable/ic_save_button_background</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>
</resources>
